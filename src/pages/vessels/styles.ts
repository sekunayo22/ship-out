import styled from '@emotion/styled'
import { background } from '../../styles/abstracts/colors'

export const VesselsContainer = styled.div`
  display: flex;
  flex-direction: column;
  gap: 16px;
  background-color: ${background};
  height: 100vh;
  overflow-y: hidden;
`